/**
 * Presentation-layer navigation between Swing windows.
 *
 * <p>Centralising the "which window comes next" decision keeps it out of both the service layer and
 * the individual views, so role-based routing can evolve without touching business logic.
 */
package com.restaurante.app.navigation;
